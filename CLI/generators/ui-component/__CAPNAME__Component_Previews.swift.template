import SwiftUI
// Add a preview per state difference (No need to add all states)

internal struct __CAPNAME__Component_Previews: PreviewProvider {
    static var previews: some View {
        __CAPNAME__ComponentWrapperView()
    }
}

internal struct __CAPNAME__ComponentWrapperView: View {
    @ObservedObject public var config = __CAPNAME__ComponentConfig()
    public var body: some View {
        __CAPNAME__Component(config: config).padding()
    }
}
