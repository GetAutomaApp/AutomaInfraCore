app = 'flyio-loki-shipper-automa-__FLY_ENVIRONMENT__'
primary_region = 'jnb'

[build]
  image = 'ghcr.io/superfly/fly-log-shipper:latest'

[services]
  http_checks = []
  internal_port = 8686

[[vm]]
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1
